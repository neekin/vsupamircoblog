<ul>
  <% photolist.each_with_index do  |photo, index| %>
  <li><div class="pp" data-index="<%=index%>"></div><%= image_tag(photo.file_url(:small),:data=>{:photoid=>photo.id,:imgsrc=>photo.file_url(:big),:cover=>photo.file_url(:thumb)})%></li>
   <% end %>
</ul>